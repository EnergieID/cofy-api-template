# --- Authentication --------------------------------------------------------
ENERGY_ID_COFY_API_TOKEN=           # Bearer token for API authentication

# --- Tariff module ---------------------------------------------------------
# ENTSOE_API_KEY=                   # https://transparency.entsoe.eu/ API key

# --- Database (only needed when using db.py) --------------------------------
# DATABASE_URL=sqlite:///./app.db   # or postgresql://user:pass@host/db

# --- Worker (only needed when using worker.py) ------------------------------
# JOB_STORAGE_URL=redis://localhost:6379
